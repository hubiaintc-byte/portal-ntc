/**
 * Tokens visuais do Design System Soberano 2026 — Grupo NTC.
 *
 * Esta é a fonte única de verdade do sistema visual (CLAUDE.md §3, §5.2).
 * Valores aqui não podem ser improvisados nem editáveis pelo CMS.
 *
 * Placeholder: a popular na próxima janela da Sprint F, em paralelo com
 * a construção dos componentes do Inventário Editorial. Estrutura:
 *   - paleta (Oxford, Pergaminho, Cardeal, Oliva, Dourado, neutros)
 *   - tipografia (Cormorant Garamond, Barlow — escalas)
 *   - espaçamento, raios (0 para estruturas), sombras editoriais
 *   - hierarquia 60·15·10·5
 */

export const tokens = {} as const;

export type Tokens = typeof tokens;
