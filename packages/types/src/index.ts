/**
 * @ntc/types — tipos compartilhados do Portal Grupo NTC.
 *
 * Inclui (a partir das próximas janelas):
 *   - tipos gerados pelo Payload CMS (payload-types.ts)
 *   - tipos de domínio editorial (Area, Programa, Evento, etc.)
 *   - contratos de API dos formulários (DAB §7)
 *
 * Placeholder.
 */

export {};
