import type { ImportMap } from "payload";

/**
 * Declaração TS para o `importMap.js` gerado pelo Payload
 * (`pnpm payload generate:importmap`).
 *
 * O arquivo `.js` é necessário (não `.ts`) porque o CLI do Payload 3
 * sobrescreve-o em build, com extensão fixa.
 */
export declare const importMap: ImportMap;
